# @ecommerce-catalog/shared-types

Shared TypeScript types and Zod schemas used by `apps/web` and `apps/api`.

**Status:** Not initialized yet. Scaffold in a follow-up step.

Source will live under `src/`.
